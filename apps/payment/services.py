"""Payment services.

Future home of: ``add_payment_method``, ``authorize_payment``,
``capture_payment``, ``refund_payment``, plus the gateway registry and the
``PaymentGateway`` protocol (PROJECT_SPEC §3.3).
"""
