"""Address serializers — input validation and output representation.

Full serializer implementation lands with the service / view iteration that
implements PROJECT_SPEC §2 operation 6 (add address).  The module is present
now so the app is importable and consistent with the module layout of all
other apps in this project.
"""
